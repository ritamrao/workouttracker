version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - gym_data:/app/gym.db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  gym_data:

